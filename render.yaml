services:
  - type: web
    name: milk-factory-backend
    env: python
    pythonVersion: 3.12
    branch: main
    root: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
